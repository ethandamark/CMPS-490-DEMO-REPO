<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.WeatherTracker" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Prevent black flash when returning from Settings -->
        <item name="android:windowBackground">@color/background_primary</item>
    </style>
</resources>