"""
database.py — DEPRECATED

All database access now uses the Supabase REST API (PostgREST) via
``httpx`` / ``requests``.  Table definitions live in ``create_schema.sql``
and the ``migrations/`` directory.

This file is kept as an empty stub so import paths that reference the
module don't break at the file-system level.
"""
