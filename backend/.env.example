# Backend Configuration
PORT=5000
DEBUG=False

# Supabase Configuration
SUPABASE_BASE_URL=http://localhost:54321
SUPABASE_API_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH

# External APIs (Proxied through this backend)
WEATHER_API_BASE=https://api.weather.gov
RAINVIEWER_API_BASE=https://api.rainviewer.com

# Firebase Cloud Messaging (Push Notifications)
# Option 1: Use environment variable with full JSON (recommended for production)
FIREBASE_CONFIG=

# Option 2: Use credentials file path (for local development)
# FIREBASE_CREDENTIALS_PATH=firebase-key.json

# ML Model Configuration (update as needed)
MODEL_PATH=./models/model.pkl

# Direct Postgres connection (used by SQLAlchemy for ML pipeline)
SUPABASE_DB_URL=postgresql://postgres:postgres@localhost:54322/postgres

# ML Predictor
ML_PREDICTOR_MODE=auto
ML_PREDICTOR_API_URL=http://localhost:8000
ML_PREDICTOR_ROOT=
ML_PREDICTOR_REQUEST_TIMEOUT_SECONDS=30

# Live Weather
LIVE_WEATHER_PROVIDER=open_meteo
LIVE_WEATHER_TIMEOUT_SECONDS=15
OPEN_METEO_BASE_URL=https://api.open-meteo.com/v1/forecast
